// Instructions
// Replace ___ with the correct code.

// 1. Fix the following code.
// Create a variable called carName and assign the value Toyota to it.
let ___ = "Toyota";

// 2. Fix the following code.
// Create a variable called x, assign the value 50 to it.
let ___ = ___;

// 3. Fix the following code.
// Display the sum of 5 + 10, using two variables: a and b.
let ___ = ___;
let b = 10;
console.log(a ___ b);

// 4. Fix the following code.
// Create a variable called z, assign x + y to it, and display it with a console.log.
let x = 5;
let y = 10;
___ ___ = x + y;
console.log(___);

// 5. Fix the following code.
// Multiply inventory with a value of 10 with price with a value of 5, and console.log the result:
// Use the * symbol to multiply
let inventory = 10
let ___ = ___;
console.log(inventory ___ ___);
